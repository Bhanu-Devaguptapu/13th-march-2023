#include<stdio.h>
main()
{
	int sum=0,n,i;
	int a[n];
	printf("Enter the value of n:");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
		sum=sum+a[i];
	}
	printf("Sum of the elements:%d",sum);
}
